services:
  topologr:
    image: ghcr.io/tobeck/topologr:latest
    # build: .  # uncomment to build from source instead
    ports:
      - "3000:3000"
    volumes:
      - topologr-data:/data
    environment:
      - DATABASE_URL=/data/topologr.db

volumes:
  topologr-data:
